import React, { useState, useEffect } from 'react';
import { StorageService } from '../services/StorageService';
import { useApp } from '../context/AppContext';
import { 
  ChevronLeft, 
  Bell, 
  Menu, 
  Check, 
  Plus, 
  Download, 
  ShieldCheck, 
  MoreVertical, 
  CreditCard, 
  Sparkles, 
  ArrowDownRight, 
  ArrowUpRight,
  X,
  Wallet,
  Bitcoin,
  Star,
  Coins
} from 'lucide-react';

interface BillingModalProps {
  onClose: () => void;
  onOpenNotifications?: () => void;
  onOpenPro?: () => void;
}

interface Transaction {
  id: string;
  title: string;
  date: string;
  type: string;
  amount: string;
  isPositive: boolean;
}

export const BillingModal: React.FC<BillingModalProps> = ({ onClose, onOpenNotifications, onOpenPro }) => {
  const { profile, depositBalance, withdrawBalance, aiCredits } = useApp();
  const [toastMessage, setToastMessage] = useState<string | null>(null);
  const [isDepositOpen, setIsDepositOpen] = useState(false);
  const [depositAmount, setDepositAmount] = useState('500');
  const [depositMethod, setDepositMethod] = useState<'card' | 'crypto' | 'stars'>('card');

  const showToast = (msg: string) => {
    setToastMessage(msg);
    setTimeout(() => setToastMessage(null), 3000);
  };

  const [transactions, setTransactions] = useState<Transaction[]>(() => 
    StorageService.getItem('rival_billing_transactions', [])
  );
  const [paymentMethods, setPaymentMethods] = useState<Array<{ id: string; name: string; info: string }>>(() =>
    StorageService.getItem('rival_payment_methods', [])
  );

  useEffect(() => {
    StorageService.setItem('rival_billing_transactions', transactions);
  }, [transactions]);

  useEffect(() => {
    StorageService.setItem('rival_payment_methods', paymentMethods);
  }, [paymentMethods]);

  const handleDepositSubmit = () => {
    const amt = parseFloat(depositAmount);
    if (isNaN(amt) || amt <= 0) {
      showToast('Введите корректную сумму');
      return;
    }

    let methodText = 'Card';
    if (depositMethod === 'crypto') methodText = 'Crypto';
    if (depositMethod === 'stars') methodText = 'Telegram Stars';

    depositBalance(amt);
    const newTx: Transaction = {
      id: Date.now().toString(),
      title: `Пополнение (${methodText})`,
      date: 'Только что',
      type: 'Balance Top Up',
      amount: `+${amt.toLocaleString()} import React, { useState, useEffect } from 'react';
import { StorageService } from '../services/StorageService';
import { useApp } from '../context/AppContext';
import { 
  ChevronLeft, 
  Bell, 
  Menu, 
  Check, 
  Plus, 
  Download, 
  ShieldCheck, 
  MoreVertical, 
  CreditCard, 
  Sparkles, 
  ArrowDownRight, 
  ArrowUpRight,
  X,
  Wallet,
  Bitcoin,
  Star,
  Coins
} from 'lucide-react';

interface BillingModalProps {
  onClose: () => void;
  onOpenNotifications?: () => void;
  onOpenPro?: () => void;
}

interface Transaction {
  id: string;
  title: string;
  date: string;
  type: string;
  amount: string;
  isPositive: boolean;
}

export const BillingModal: React.FC<BillingModalProps> = ({ onClose, onOpenNotifications, onOpenPro }) => {
  const { profile, depositBalance, withdrawBalance, aiCredits } = useApp();
  const [toastMessage, setToastMessage] = useState<string | null>(null);
  const [isDepositOpen, setIsDepositOpen] = useState(false);
  const [depositAmount, setDepositAmount] = useState('500');
  const [depositMethod, setDepositMethod] = useState<'card' | 'crypto' | 'stars'>('card');

  const showToast = (msg: string) => {
    setToastMessage(msg);
    setTimeout(() => setToastMessage(null), 3000);
  };

  const [transactions, setTransactions] = useState<Transaction[]>(() => 
    StorageService.getItem('rival_billing_transactions', [])
  );
  const [paymentMethods, setPaymentMethods] = useState<Array<{ id: string; name: string; info: string }>>(() =>
    StorageService.getItem('rival_payment_methods', [])
  );

  useEffect(() => {
    StorageService.setItem('rival_billing_transactions', transactions);
  }, [transactions]);

  useEffect(() => {
    StorageService.setItem('rival_payment_methods', paymentMethods);
  }, [paymentMethods]);

  ,
      isPositive: true
    };

    setTransactions(prev => [newTx, ...prev]);
    setIsDepositOpen(false);
    showToast(`Успешно пополнено через ${methodText}`);
  };

  return (
    <div className="fixed inset-0 z-50 overflow-y-auto bg-[#050508] font-sans animate-in fade-in duration-200">
      <div className="max-w-md mx-auto min-h-screen px-4 py-6 space-y-6 pb-28">
        
        {/* Toast Notification */}
        {toastMessage && (
          <div className="fixed top-4 left-1/2 -translate-x-1/2 z-50 bg-indigo-600 text-white font-mono text-xs px-4 py-2 rounded-full shadow-2xl border border-indigo-400 flex items-center gap-2 animate-bounce">
            <Sparkles className="w-4 h-4 text-yellow-300" />
            <span>{toastMessage}</span>
          </div>
        )}

        {/* TOP HEADER BAR (Exact like screenshot) */}
        <div className="flex items-center justify-between pt-1">
          <button
            onClick={onClose}
            className="w-10 h-10 rounded-full bg-zinc-900 border border-zinc-800 text-zinc-300 hover:text-white hover:bg-zinc-800 transition-colors flex items-center justify-center active:scale-95"
            aria-label="Back"
          >
            <ChevronLeft className="w-5 h-5" />
          </button>

          <span className="text-xs font-mono font-bold tracking-widest text-zinc-300 uppercase">
            RIVAL SPACE
          </span>

          <button
            onClick={() => {
              if (onOpenNotifications) {
                onClose();
                onOpenNotifications();
              } else {
                showToast('Уведомления');
              }
            }}
            className="w-10 h-10 rounded-full bg-zinc-900 border border-zinc-800 text-zinc-300 hover:text-white hover:bg-zinc-800 transition-colors flex items-center justify-center active:scale-95 relative"
            aria-label="Notifications"
          >
            <Bell className="w-4 h-4" />
            <span className="absolute top-2.5 right-2.5 w-2 h-2 rounded-full bg-indigo-500 shadow-[0_0_6px_rgba(99,102,241,0.9)]" />
          </button>
        </div>

        {/* PAGE TITLE & SUBTITLE */}
        <div className="space-y-1 px-1">
          <h1 className="text-2xl sm:text-3xl font-extrabold text-white tracking-tight">
            Billing
          </h1>
          <p className="text-xs sm:text-sm text-zinc-400 leading-relaxed">
            Manage your payments and subscription.
          </p>
        </div>

        {/* TOTAL BALANCE CARD */}
        <div className="p-6 rounded-3xl bg-gradient-to-b from-[#121222] via-[#0e0e1a] to-[#0b0b14] border border-indigo-500/30 shadow-xl space-y-5">
          <div className="space-y-1">
            <span className="text-[10px] font-mono font-bold tracking-widest text-zinc-400 uppercase">
              TOTAL BALANCE
            </span>
            <div className="text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
              ${profile.balance.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
            </div>
          </div>

          <div className="flex items-center gap-3">
            <div className="flex items-center gap-1.5 text-xs text-emerald-400 font-medium">
              <span className="w-2 h-2 rounded-full bg-emerald-400 animate-pulse" />
              <span>Available funds</span>
            </div>

            <span className="px-2.5 py-0.5 rounded-full bg-indigo-950/80 border border-indigo-500/30 text-indigo-300 text-[10px] font-mono font-semibold">
              {aiCredits} AI Credits
            </span>
          </div>

          <div className="grid grid-cols-2 gap-3 pt-1">
            <button
              onClick={() => setIsDepositOpen(true)}
              className="py-3 rounded-2xl bg-indigo-600 hover:bg-indigo-500 text-white font-bold text-xs uppercase tracking-wider shadow-lg shadow-indigo-600/30 transition-all active:scale-95 text-center"
            >
              Deposit
            </button>

            <button
              onClick={() => {
                if (profile.balance <= 0) {
                  showToast('Недостаточно средств для вывода');
                  return;
                }
                const amt = Math.min(1000, profile.balance);
                const ok = withdrawBalance(amt);
                if (ok) {
                  const newTx: Transaction = {
                    id: Date.now().toString(),
                    title: 'Withdrawal',
                    date: 'Just now',
                    type: 'Payout',
                    amount: `-${amt.toLocaleString()} ₽`,
                    isPositive: false
                  };
                  setTransactions(prev => [newTx, ...prev]);
                }
              }}
              className="py-3 rounded-2xl bg-zinc-900/90 hover:bg-zinc-800 border border-zinc-700/80 text-zinc-200 font-bold text-xs uppercase tracking-wider transition-all active:scale-95 text-center"
            >
              Withdraw
            </button>
          </div>
        </div>

        {/* SUBSCRIPTION CARD */}
        <div className="p-6 rounded-3xl bg-[#0e0e16] border border-zinc-800/90 shadow-xl space-y-4">
          <div className="flex items-start justify-between">
            <div className="w-10 h-10 rounded-2xl bg-indigo-950/60 border border-indigo-500/40 flex items-center justify-center text-indigo-400">
              <Sparkles className="w-5 h-5" />
            </div>

            <span className="px-3 py-1 rounded-full bg-indigo-950/80 text-indigo-300 border border-indigo-500/40 text-[10px] font-mono font-bold uppercase tracking-wider">
              ACTIVE
            </span>
          </div>

          <div>
            <h3 className="text-lg font-extrabold text-white tracking-tight">
              Rival Space Pro
            </h3>
            <p className="text-xs font-mono text-zinc-400 mt-0.5">
              $29 / month
            </p>
          </div>

          <div className="space-y-2 text-xs text-zinc-300 pt-1">
            <div className="flex items-center gap-2">
              <Check className="w-4 h-4 text-indigo-400 shrink-0" />
              <span>Unlimited AI Assistant</span>
            </div>
            <div className="flex items-center gap-2">
              <Check className="w-4 h-4 text-indigo-400 shrink-0" />
              <span>Premium Assets</span>
            </div>
            <div className="flex items-center gap-2">
              <Check className="w-4 h-4 text-indigo-400 shrink-0" />
              <span>Advanced Analytics</span>
            </div>
            <div className="flex items-center gap-2">
              <Check className="w-4 h-4 text-indigo-400 shrink-0" />
              <span>Priority Support</span>
            </div>
          </div>

          <button
            onClick={() => {
              if (onOpenPro) {
                onClose();
                onOpenPro();
              } else {
                showToast('Настройки подписки Pro');
              }
            }}
            className="w-full py-3.5 rounded-2xl bg-zinc-900/90 hover:bg-zinc-800 border border-zinc-700/80 text-zinc-200 font-bold text-xs uppercase tracking-wider transition-all active:scale-95 text-center mt-2"
          >
            Manage Subscription
          </button>
        </div>

        {/* PAYMENT METHODS SECTION */}
        <div className="space-y-3">
          <h3 className="text-sm font-bold text-white tracking-tight px-1">
            Payment Methods
          </h3>

          <div className="space-y-2">
            {/* Visa */}
            <div className="p-4 rounded-2xl bg-[#0e0e16] border border-zinc-800/90 flex items-center justify-between">
              <div className="flex items-center gap-3.5">
                <div className="w-12 h-8 rounded-lg bg-zinc-900 border border-zinc-700 flex items-center justify-center font-bold italic text-indigo-400 text-xs tracking-wider">
                  VISA
                </div>
                <div>
                  <h4 className="text-xs font-bold text-white">Visa •••• 4821</h4>
                  <p className="text-[10px] font-mono text-indigo-400 font-semibold">Primary</p>
                </div>
              </div>

              <button onClick={() => showToast('Настройки карты')} className="text-zinc-500 hover:text-zinc-300 p-1">
                <MoreVertical className="w-4 h-4" />
              </button>
            </div>

            {/* Crypto Wallet */}
            <div className="p-4 rounded-2xl bg-[#0e0e16] border border-zinc-800/90 flex items-center justify-between">
              <div className="flex items-center gap-3.5">
                <div className="w-12 h-8 rounded-lg bg-zinc-900 border border-zinc-700 flex items-center justify-center text-amber-400 font-mono font-bold text-xs">
                  ₿
                </div>
                <div>
                  <h4 className="text-xs font-bold text-white">Crypto Wallet</h4>
                  <p className="text-[10px] font-mono text-zinc-400">Connected</p>
                </div>
              </div>

              <button onClick={() => showToast('Настройки криптокошелька')} className="text-zinc-500 hover:text-zinc-300 p-1">
                <MoreVertical className="w-4 h-4" />
              </button>
            </div>

            {/* PayPal */}
            <div className="p-4 rounded-2xl bg-[#0e0e16] border border-zinc-800/90 flex items-center justify-between">
              <div className="flex items-center gap-3.5">
                <div className="w-12 h-8 rounded-lg bg-zinc-900 border border-zinc-700 flex items-center justify-center text-blue-400 font-bold italic text-xs">
                  P
                </div>
                <div>
                  <h4 className="text-xs font-bold text-white">PayPal</h4>
                  <p className="text-[10px] font-mono text-zinc-400">Connected</p>
                </div>
              </div>

              <button onClick={() => showToast('Настройки PayPal')} className="text-zinc-500 hover:text-zinc-300 p-1">
                <MoreVertical className="w-4 h-4" />
              </button>
            </div>

            {/* Add Method Button */}
            <button
              onClick={() => showToast('Добавление нового способа оплаты')}
              className="w-full py-3.5 rounded-2xl border-2 border-dashed border-zinc-800 hover:border-zinc-700 bg-zinc-900/30 text-xs font-semibold text-zinc-300 flex items-center justify-center gap-2 transition-all active:scale-95"
            >
              <Plus className="w-4 h-4 text-indigo-400" />
              <span>Add Method</span>
            </button>
          </div>
        </div>

        {/* RECENT TRANSACTIONS */}
        <div className="space-y-3">
          <div className="flex items-center justify-between px-1">
            <h3 className="text-sm font-bold text-white tracking-tight">
              Recent Transactions
            </h3>
            <button 
              onClick={() => showToast('Все транзакции')}
              className="text-[11px] font-mono font-bold text-indigo-400 hover:text-indigo-300 uppercase tracking-wider"
            >
              VIEW ALL
            </button>
          </div>

          {transactions.length === 0 ? (
            <div className="p-6 rounded-3xl bg-[#0e0e16] border border-zinc-800/90 text-center space-y-2 shadow-xl">
              <div className="w-10 h-10 rounded-2xl bg-indigo-950/60 border border-indigo-500/30 flex items-center justify-center text-indigo-400 mx-auto">
                <CreditCard className="w-5 h-5" />
              </div>
              <h4 className="text-xs font-bold text-white">Транзакций пока нет</h4>
              <p className="text-[11px] text-zinc-400">
                После пополнения баланса или оплаты заказов здесь появится их история.
              </p>
            </div>
          ) : (
            <div className="p-5 rounded-3xl bg-[#0e0e16] border border-zinc-800/90 shadow-xl divide-y divide-zinc-800/70">
              {transactions.map(tx => (
                <div key={tx.id} className="py-3.5 first:pt-0 last:pb-0 flex items-center justify-between">
                  <div className="flex items-center gap-3.5">
                    <div className={`w-9 h-9 rounded-2xl flex items-center justify-center shrink-0 border ${
                      tx.isPositive 
                        ? 'bg-emerald-950/60 border-emerald-500/40 text-emerald-400' 
                        : 'bg-zinc-900 border-zinc-800 text-zinc-400'
                    }`}>
                      {tx.isPositive ? (
                        <ArrowUpRight className="w-4 h-4" />
                      ) : (
                        <ArrowDownRight className="w-4 h-4" />
                      )}
                    </div>

                    <div>
                      <h4 className="text-xs font-bold text-white leading-snug">
                        {tx.title}
                      </h4>
                      <p className="text-[10px] text-zinc-400 font-mono mt-0.5">
                        {tx.date} • {tx.type}
                      </p>
                    </div>
                  </div>

                  <span className={`text-xs font-mono font-bold ${
                    tx.isPositive ? 'text-emerald-400' : 'text-zinc-200'
                  }`}>
                    {tx.amount}
                  </span>
                </div>
              ))}
            </div>
          )}
        </div>

        {/* INVOICES SECTION */}
        <div className="space-y-3">
          <h3 className="text-sm font-bold text-white tracking-tight px-1">
            Invoices
          </h3>

          <div className="space-y-2">
            <div className="p-4 rounded-2xl bg-[#0e0e16] border border-zinc-800/90 flex items-center justify-between">
              <div>
                <h4 className="text-xs font-bold text-white">October 2026</h4>
                <p className="text-[10px] text-zinc-400 font-mono">Project Cyber-Interface</p>
              </div>

              <button 
                onClick={() => showToast('Загрузка инвойса за Октябрь 2026')}
                className="p-2 text-zinc-400 hover:text-white transition-colors"
              >
                <Download className="w-4 h-4" />
              </button>
            </div>

            <div className="p-4 rounded-2xl bg-[#0e0e16] border border-zinc-800/90 flex items-center justify-between">
              <div>
                <h4 className="text-xs font-bold text-white">September 2026</h4>
                <p className="text-[10px] text-zinc-400 font-mono">Rival Space Pro</p>
              </div>

              <button 
                onClick={() => showToast('Загрузка инвойса за Сентябрь 2026')}
                className="p-2 text-zinc-400 hover:text-white transition-colors"
              >
                <Download className="w-4 h-4" />
              </button>
            </div>
          </div>

          <div className="px-1">
            <button
              onClick={() => showToast('Скачивание всех инвойсов PDF')}
              className="text-xs font-mono font-semibold text-indigo-400 hover:text-indigo-300 flex items-center gap-1 transition-colors"
            >
              <span>Download All PDF</span>
              <span>→</span>
            </button>
          </div>
        </div>

        {/* BOTTOM SECURE PAYMENTS BANNER */}
        <div className="p-4 rounded-2xl bg-[#0a0a10] border border-zinc-800/80 flex items-start gap-3">
          <ShieldCheck className="w-5 h-5 text-indigo-400 shrink-0 mt-0.5" />
          <div className="space-y-0.5">
            <h4 className="text-xs font-bold text-white">Secure Payments</h4>
            <p className="text-[11px] text-zinc-400 leading-relaxed">
              Your payment information is encrypted and protected.
            </p>
          </div>
        </div>

      </div>

      {/* DEPOSIT MODAL POPUP */}
      {isDepositOpen && (
        <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm animate-in fade-in">
          <div className="w-full max-w-sm bg-[#0e0e18] border border-zinc-800 rounded-3xl p-6 space-y-5 text-white animate-in zoom-in-95">
            <div className="flex items-center justify-between border-b border-zinc-800/80 pb-3">
              <h3 className="text-base font-bold">Пополнение баланса</h3>
              <button onClick={() => setIsDepositOpen(false)} className="text-zinc-500 hover:text-zinc-300">
                <X className="w-5 h-5" />
              </button>
            </div>

            <div className="space-y-3">
              <label className="text-xs text-zinc-400 font-mono uppercase tracking-wider">Способ оплаты</label>
              <div className="grid grid-cols-3 gap-2">
                <button
                  onClick={() => setDepositMethod('card')}
                  className={`flex flex-col items-center justify-center gap-2 p-3 rounded-2xl border transition-all ${depositMethod === 'card' ? 'bg-indigo-600/20 border-indigo-500 text-indigo-400' : 'bg-zinc-900 border-zinc-800 text-zinc-400 hover:border-zinc-700'}`}
                >
                  <CreditCard className="w-5 h-5" />
                  <span className="text-[10px] font-bold uppercase">Карта</span>
                </button>
                <button
                  onClick={() => setDepositMethod('crypto')}
                  className={`flex flex-col items-center justify-center gap-2 p-3 rounded-2xl border transition-all ${depositMethod === 'crypto' ? 'bg-indigo-600/20 border-indigo-500 text-indigo-400' : 'bg-zinc-900 border-zinc-800 text-zinc-400 hover:border-zinc-700'}`}
                >
                  <Bitcoin className="w-5 h-5" />
                  <span className="text-[10px] font-bold uppercase">Крипта</span>
                </button>
                <button
                  onClick={() => setDepositMethod('stars')}
                  className={`flex flex-col items-center justify-center gap-2 p-3 rounded-2xl border transition-all ${depositMethod === 'stars' ? 'bg-indigo-600/20 border-indigo-500 text-indigo-400' : 'bg-zinc-900 border-zinc-800 text-zinc-400 hover:border-zinc-700'}`}
                >
                  <Star className="w-5 h-5" />
                  <span className="text-[10px] font-bold uppercase">Звёзды</span>
                </button>
              </div>
            </div>

            <div className="space-y-3">
              <label className="text-xs text-zinc-400 font-mono uppercase tracking-wider">Сумма (USD)</label>
              <div className="relative">
                <input
                  type="number"
                  value={depositAmount}
                  onChange={(e) => setDepositAmount(e.target.value)}
                  className="w-full pl-8 pr-4 py-3 rounded-2xl bg-zinc-900 border border-zinc-700 text-lg font-bold text-white focus:outline-none focus:border-indigo-500 transition-colors"
                />
                <span className="absolute left-4 top-1/2 -translate-y-1/2 text-zinc-400 font-bold">$</span>
              </div>
            </div>

            <div className="grid grid-cols-3 gap-2">
              {['100', '500', '1000'].map(val => (
                <button
                  key={val}
                  onClick={() => setDepositAmount(val)}
                  className="py-2 rounded-xl bg-zinc-900 border border-zinc-800 text-xs font-mono font-bold hover:border-indigo-500 hover:text-indigo-400 text-zinc-300 transition-colors"
                >
                  +${val}
                </button>
              ))}
            </div>

            <button
              onClick={handleDepositSubmit}
              className="w-full py-4 mt-2 rounded-2xl bg-indigo-600 hover:bg-indigo-500 text-white font-bold text-sm uppercase tracking-wider shadow-xl shadow-indigo-600/30 transition-transform active:scale-95"
            >
              Оплатить {depositAmount || 0}$
            </button>
          </div>
        </div>
      )}
    </div>
  );
};
